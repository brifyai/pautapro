/**
 * @fileoverview Pruebas unitarias para el componente RentabilidadDashboard
 */
import { describe, it, expect } from 'vitest';

describe('RentabilidadDashboard', () => {
  it('debe pasar una prueba básica', () => {
    expect(1).toBe(1);
  });
});